// variable types
// var, let, const
// var - global scope
//let - block scope
//const - constant. cannot change the value once assigned.

var score = 10;
let max = 15;
const pi = 3.14;

// Data types
// String, number, boolean, null, undefined

const name = 'Adam';
const age = 25;
const isAdult = true;
const car = null;
const z = undefined;

// Concatenation
console.log('My Name is ' + name + ' and I am '+ age);

// Template String
console.log(`My Name is ${name} and I am ${age}.`);

// String Methods
const s = 'Hello World';
console.log(s.toUpperCase());
console.log(s.toLowerCase());
console.log(s.substring(1,5));
console.log(s.split(' '));